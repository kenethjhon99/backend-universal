# Copia este archivo a .env.test y apuntalo a una BD vacia exclusiva para tests.
# IMPORTANTE: nunca apuntes a la BD de produccion. Los tests crean y dropean schemas temporales.
PGHOST=localhost
PGPORT=5432
PGDATABASE=pos_saas_test
PGUSER=postgres
PGPASSWORD=postgres
PGSSLMODE=disable
